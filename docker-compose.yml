version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.backend
    container_name: django-backend
    restart: unless-stopped
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb+srv://Shashidhar:Shashi2002@cluster0.gcjaelb.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - GROQ_API_KEY=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
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.frontend
    container_name: lanchain-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"

  mongo:
    image: mongo:latest
    container_name: lanchain-mongo-1
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:

networks:
  default:
    name: lanchain_default
